machine:
  ruby:
    version: 2.2.3

test:
  override:
    - bundle exec rubocop
      --config .rubocop.yml
    - bundle exec haml-lint app/views
    - bundle exec rspec spec
      --format documentation
