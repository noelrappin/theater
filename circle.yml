machine:
  ruby:
    version: 2.2.3

dependencies:
  post:
    - npm install eslint
    - npm install babel-eslint

test:
  override:
    - eslint app/assets/javascripts/**.es6
    - bundle exec rubocop
      --config .rubocop.yml
    - bundle exec rspec spec
      --format documentation
