machine:
  ruby:
    version: 2.2.3

test:
  override:
    - eslint app/assets/javascripts/**.es6
    - bundle exec rubocop
      --config .rubocop.yml
    - bundle exec rspec spec
      --format documentation
